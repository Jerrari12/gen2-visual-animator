/* =========================================================================
   FILAMENT PROFILES — the authored per-label finish, two scalars deep
   =========================================================================
   SOURCE OF TRUTH: filament-material-lab/profiles/*.json
   SOURCE SHA256:   c21320b129faeb3c428a17216e466a3e75201038c58ca0a3d47aaf02acac5041
   GENERATED BY:    filament-material-lab/tools/build-filament-profiles.mjs

   ⚠ DERIVED FILE. DO NOT EDIT IT HERE, AND DO NOT EDIT IT THERE EITHER.
   Edit the profile JSON in the Lab and re-run the generator, which rewrites
   this file in both repositories and re-stamps the hash above.

   ⚠ OWNERSHIP IS INVERTED FROM part-material.js, WHICH IMPORTS THIS.
   Build Studio owns the finish spec and the key vocabulary — a filament lands
   in FILAMENT_DB first, always — and the Lab owns the per-label values. So the
   two halves of the drift gate swap sides on this one import:

     HERE   a hand-edit to this copy       -> a pinned hash in the viewer's suite
     LAB    a profile edited with no       -> tools/build-filament-profiles.mjs
            re-emit                           --check, and their unit gate

   ⚠ A PROFILE FIRES UNDER A LABEL ONLY. In the viewer a filament label exists
   only where the user has assigned a custom palette. In instruction-colours
   mode — the default — and in the ?part= embed there is no label, no profile
   resolves, and the part renders the base finish plus layer detail. That is a
   decision taken 2026-09-07, not an omission.

   ⚠ IT IMPORTS NOTHING. Plain data, loadable in a browser and in plain node on
   both sides, which is what lets part-material.js stay testable in either.
   ========================================================================= */

/**
 * Per-filament-label finish overrides, keyed by the label `FILAMENT_DB` owns.
 *
 * @type {Readonly<Record<string, { roughness: number, metalness: number }>>}
 */
export const FILAMENT_PROFILES = Object.freeze({
  /* fl-ref-1--printed-solid-mystery-orange.json — authored on fl-ref-1 */
  "Printed Solid Mystery Orange": Object.freeze({ roughness: 0.34, metalness: 0 }),
});

/** How many labels carry a profile. Zero is a legitimate state and says so. */
export const FILAMENT_PROFILE_COUNT = 1;
